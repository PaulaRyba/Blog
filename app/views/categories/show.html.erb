<p id="notice"><%= notice %></p>

<h1><%= @category.name %><small><%= link_to 'Back', categories_path %></small></h1>

    <% @category.posts.each do |post| %>
    <article>

        <h3><%= link_to post.title, [post.category, post] %></h3>
        <h6>Written by <a href="#">Rybałke</a> on <%= post.created_at %></h6>
       
        <p><%= post.content %></p>

      </article>

      <hr />

    <% end %>
